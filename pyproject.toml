[project]
name = "maccare"
version = "0.1.0"
description = "macOS Agentic Health Assistant"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ollama>=0.2.0",
    "psutil>=5.9.0",
    "pydantic-ai>=0.0.14",
    "pydantic>=2.0.0",
    "rich>=13.7.0",
]

[tool.uv]
package = false
